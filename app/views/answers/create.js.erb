$('.answer-errors').html('<%= j  render 'shared/errors', resource: answer %>');
$('.notice').html('<%= flash[:notice] %>');
<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer%>');
  $('.new-answer textarea').val('');
<% end %>