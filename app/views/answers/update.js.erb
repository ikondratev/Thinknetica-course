$('.answer-error-<%=answer.id%>').html('<%= j  render 'shared/errors', resource: answer %>');
<% if !answer.errors.present? %>
  $('#answer-<%= answer.id %>').replaceWith('<%= j render answer %>');
<% end %>	